---
title: Gateways
description: Fourteen VE-face neighbours are capacity gateways. Every handle bit is a usable mask.
outline: deep
---

# Gateways <Badge type="tip" text="14 neighbours" />

Fourteen neighbours unlock capacity. Masks are `qpuHandleMaskOf(bits)` for every bit on the handle. JSON is `qpuGatewaysOf()`. Face 0 stays at [Face 0](/face/0). Formula versus peer stays at [Metrics](/metrics). Speed rungs stay at [Speed](/speed).

::: info JSON
`curl -sS https://qpu.uuidna.com/gateways` · `curl -sS https://qpu.uuidna.com/face/0`
:::
