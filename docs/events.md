---
title: QPU events
description: Fourteen UI event kinds occupy the VE faces. Capture imprints. Hardware QPU lane stays empty.
outline: deep
---

# QPU events

Fourteen UI event kinds occupy the VE faces. Capture, passive — the movie imprints, the page still handles. Live occupancy stays at [Live](/live).

## Kinds · {{ reading.kinds.length }}

{{ reading.kinds.map(k => k.kind).join(' · ') }}

## Listen

{{ reading.listen.join(' · ') }}

::: info JSON
`curl -sS https://qpu.uuidna.com/events`
:::
