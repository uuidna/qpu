---
title: Experience
description: Inner CPU clockwise and outer GPU counterclockwise are one involution. Glow 24 and 64. Both fuse at residue 0.
outline: deep
---

# Experience <Badge type="tip" text="involution" />

Inner `{{ reading.inner.name }}` rotor `{{ reading.inner.rotor }}` spin `{{ reading.inner.spin }}` glow `{{ reading.inner.glow }}`.

Outer `{{ reading.outer.name }}` rotor `{{ reading.outer.rotor }}` spin `{{ reading.outer.spin }}` glow `{{ reading.outer.glow }}`.

Fuse `{{ reading.fuse }}`. Involution `{{ reading.involution }}`.

The live occupancy movie is the viewport. See **[Live](/live)**.

[`qpu_inner_and_outer_experience`](https://uuidna.com/theorem/qpu_inner_and_outer_experience)

::: info JSON
`curl -sS https://qpu.uuidna.com/experience` · `curl -sS https://qpu.uuidna.com/live`
:::
